import streamlit as st
st.set_page_config(page_title="AI Based Study Planner", layout="wide")

git init
git add .
git commit -m "Initial AI-driven study planner app"
git branch -M main
git remote add origin https://github.com/<SagnikChunder>/ai-study-planner.git
git push -u origin main
